# Proyecto Flask + PostgreSQL con Docker

Este proyecto contiene una aplicación Flask que se conecta a una base de datos PostgreSQL usando Docker.

## Cómo ejecutar

1. Clona el repositorio
2. Ejecuta:

```bash
docker-compose up --build
